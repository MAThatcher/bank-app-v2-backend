const express = require('express');
const router = express.Router();
//const { authenticateToken } = require('../services/AuthService');
//const SessionsController = require('../controllers/sessions.controller');

// router.get('/sessions', authenticateToken, UsersController.getActiveSessions);
// router.delete('/sessions/:sessionId', authenticateToken, UsersController.revokeSessionById);
// router.post('/2fa/setup', authenticateToken, UsersController.setupTwoFactorAuth);
// router.post('/2fa/verify', authenticateToken, UsersController.verifyTwoFactorAuth);
// router.post('/2fa/disable', authenticateToken, UsersController.disableTwoFactorAuth);

module.exports = router;
